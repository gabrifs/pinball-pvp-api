name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read
  packages: write

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore

      - name: Test
        run: dotnet test --no-build

  docker:
    needs: build-and-test
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=sha-

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Deploy ────────────────────────────────────────────────────────────────────
  # Runs on a GitHub-hosted runner and deploys to the Windows PC via Docker's SSH
  # context backend, tunneled entirely over Tailscale — the host has no inbound
  # ports open to the public internet (its residential ISP uses CGNAT, which
  # makes that impossible anyway; Tailscale's outbound-only connection sidesteps
  # it). The api service itself is separately published to the internet on the
  # host via `tailscale funnel` (see .claude/Contexts/deployment.md), not through
  # anything in this workflow.
  # Prerequisites on the host:
  #   1. Install Tailscale on the host and join it to this project's tailnet.
  #   2. Enable Windows OpenSSH Server (Settings → Optional Features → OpenSSH Server).
  #   3. Generate an Ed25519 key pair and add the public key to
  #      C:\Users\<user>\.ssh\authorized_keys on the host.
  #   4. Set DEPLOY_SSH_KEY (secret), DEPLOY_HOST (host's Tailscale IP or MagicDNS
  #      name), DEPLOY_SSH_PORT (22), DEPLOY_SSH_USER, TS_OAUTH_CLIENT_ID,
  #      TS_OAUTH_SECRET in Settings → Secrets and variables → Actions.
  #   5. Set all app secrets listed in the step env blocks below.
  #   6. Docker Desktop must be running on the host; docker must be in PATH for the
  #      SSH user (Docker Desktop ensures this).
  deploy:
    needs: docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Connect to Tailscale
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Compute image tag
        id: tag
        shell: bash
        run: |
          sha="${{ github.sha }}"
          echo "value=sha-${sha:0:7}" >> "$GITHUB_OUTPUT"

      - name: Set up SSH
        shell: bash
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "${{ vars.DEPLOY_SSH_PORT }}" "${{ vars.DEPLOY_HOST }}" >> ~/.ssh/known_hosts

      - name: Create Docker context
        shell: bash
        run: |
          docker context create production \
            --docker "host=ssh://${{ vars.DEPLOY_SSH_USER }}@${{ vars.DEPLOY_HOST }}:${{ vars.DEPLOY_SSH_PORT }}" \
            --description "Production host"

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull new images
        shell: bash
        run: docker --context production compose pull api migrate
        env:
          IMAGE_TAG: ${{ steps.tag.outputs.value }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          ConnectionStrings__DefaultConnection: ${{ secrets.CONNECTION_STRING }}
          Jwt__Key: ${{ secrets.JWT_KEY }}
          Jwt__Issuer: ${{ secrets.JWT_ISSUER }}
          Jwt__Audience: ${{ secrets.JWT_AUDIENCE }}
          Jwt__ExpirationMinutes: ${{ secrets.JWT_EXPIRATION_MINUTES }}
          Jwt__RefreshTokenExpirationDays: ${{ secrets.JWT_REFRESH_TOKEN_EXPIRATION_DAYS }}
          Email__Host: ${{ secrets.EMAIL_HOST }}
          Email__Port: ${{ secrets.EMAIL_PORT }}
          Email__FromAddress: ${{ secrets.EMAIL_FROM_ADDRESS }}
          Email__FromName: ${{ secrets.EMAIL_FROM_NAME }}
          Email__Username: ${{ secrets.EMAIL_USERNAME }}
          Email__Password: ${{ secrets.EMAIL_PASSWORD }}
          Cors__AllowedOrigins__0: ${{ secrets.CORS_ALLOWED_ORIGIN_0 }}
          Leaderboard__WinRateMinMatches: ${{ vars.LEADERBOARD_WIN_RATE_MIN_MATCHES }}
          Maintenance__PurgeIntervalHours: ${{ vars.MAINTENANCE_PURGE_INTERVAL_HOURS }}
          PasswordRecovery__ExpirationMinutes: ${{ vars.PASSWORD_RECOVERY_EXPIRATION_MINUTES }}

      - name: Deploy (migrate + start services)
        shell: bash
        run: docker --context production compose up -d --wait
        env:
          IMAGE_TAG: ${{ steps.tag.outputs.value }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          ConnectionStrings__DefaultConnection: ${{ secrets.CONNECTION_STRING }}
          Jwt__Key: ${{ secrets.JWT_KEY }}
          Jwt__Issuer: ${{ secrets.JWT_ISSUER }}
          Jwt__Audience: ${{ secrets.JWT_AUDIENCE }}
          Jwt__ExpirationMinutes: ${{ secrets.JWT_EXPIRATION_MINUTES }}
          Jwt__RefreshTokenExpirationDays: ${{ secrets.JWT_REFRESH_TOKEN_EXPIRATION_DAYS }}
          Email__Host: ${{ secrets.EMAIL_HOST }}
          Email__Port: ${{ secrets.EMAIL_PORT }}
          Email__FromAddress: ${{ secrets.EMAIL_FROM_ADDRESS }}
          Email__FromName: ${{ secrets.EMAIL_FROM_NAME }}
          Email__Username: ${{ secrets.EMAIL_USERNAME }}
          Email__Password: ${{ secrets.EMAIL_PASSWORD }}
          Cors__AllowedOrigins__0: ${{ secrets.CORS_ALLOWED_ORIGIN_0 }}
          Leaderboard__WinRateMinMatches: ${{ vars.LEADERBOARD_WIN_RATE_MIN_MATCHES }}
          Maintenance__PurgeIntervalHours: ${{ vars.MAINTENANCE_PURGE_INTERVAL_HOURS }}
          PasswordRecovery__ExpirationMinutes: ${{ vars.PASSWORD_RECOVERY_EXPIRATION_MINUTES }}
